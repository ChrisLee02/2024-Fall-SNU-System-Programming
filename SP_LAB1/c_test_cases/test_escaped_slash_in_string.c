#include <stdio.h>

int main() {
    printf("This is a string with a forward slash: / and an escaped slash: \/.
");
    printf("None of these slashes should be considered comments.
");
    return 0;
}
