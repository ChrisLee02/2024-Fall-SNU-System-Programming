#include <stdio.h>

int main() {
    /* Comment with special characters: !@#$%^&*() and whitespaces
       tabs 		and newlines
    */
    printf("Special characters inside comments should be handled correctly.
");
    return 0;
}
