#include <stdio.h>

int main() {
    /* This is a multi-line comment
    * with several * asterisks inserted *
    * in various places. */
    printf("This should run normally if comments are removed correctly.
");
    return 0;
}
